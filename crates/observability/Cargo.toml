[package]
name = "website-observability"
version = "0.1.0"
description = "Observability setup for web services"
license.workspace = true
edition.workspace = true

[dependencies]
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
