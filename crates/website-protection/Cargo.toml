[package]
name = "website-protection"
version = "0.1.0"
description = "Security headers, origin checks, and rate limiting primitives"
license.workspace = true
edition.workspace = true

[dependencies]
std-semaphore = "0.1"
